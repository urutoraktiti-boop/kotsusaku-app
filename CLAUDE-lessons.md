# Claude 失敗記録・原因・対策

作業前に必ず読むこと。同じミスを繰り返さないための記録。

---

## #001 · 2026-06-03 · 古いコードを「最新」として分析した

### 何が起きたか
ユーザーがローカル（自分のパソコン）で修正済みのコードについて質問した。
Claudeはリモート（GitHub上）の古いコードを見て「`kotsusaku-kiseki-cache` というキーは存在しない」と断言した。
しかし実際にはユーザーのローカルにすでに実装されており、回答が誤りだった。

### 原因
- 作業開始前に `git fetch` でGitHubの最新情報を取得しなかった
- ユーザーが「実装した」と言ったにもかかわらず、GitHubへの反映状況を確認しなかった
- リポジトリにあるコードをそのまま「最新版」と思い込んだ

### 対策
- GitHubのファイルに関する作業・質問・分析を始める前に、**毎回必ず**以下を実行する
  ```bash
  git fetch origin
  git log --oneline -5
  git status
  ```
- ユーザーが「実装した」「修正した」「変更した」と言ったら、GitHubに反映済みか確認してから回答する
- 確認せずに断定的な回答をしない

---

## #002 · 2026-06-03 · 専門用語を説明なしで使い続けた

### 何が起きたか
「プッシュ」「ブランチ」「コミット」「デプロイ」などの専門用語を、説明なしで繰り返し使い続けた。
ユーザーは非エンジニアであり、会話の中で何度も指摘を受けていたにもかかわらず徹底できていなかった。

### 原因
- セッションをまたぐと記憶がリセットされるため、以前の指摘が引き継がれなかった
- 「分かるだろう」という思い込みで説明を省略した

### 対策
- 専門用語を使う際は**必ず括弧書きで平易な説明を添える**
  - 例：「プッシュ（自分のパソコンからGitHubに変更を送り込む作業）」
  - 例：「ブランチ（作業用の分岐・コピー）」
  - 例：「コミット（変更内容をひとまとまりとして記録すること）」
  - 例：「デプロイ（作ったものを本番環境に反映させる作業）」
- 技術的な説明はたとえ話や日常語に置き換える

---

## #003 · 2026-06-03 · コマンド例にフォルダ移動（cd）を含めなかった

### 何が起きたか
「フォルダに移動してから実行してください」と文章では説明したにもかかわらず、
コマンド例として `firebase deploy --only functions` の1行だけを提示した。
ターミナル（コマンド入力画面）を開いた直後は別のフォルダにいるため、
移動コマンド（`cd`）を含めないと正しく実行できない。

### 原因
- 「文章で説明したから伝わるだろう」という思い込みでコマンド例を省略した
- 非エンジニアのユーザーにとって、文章の説明とコマンド例が別々だと混乱しやすいことへの配慮が足りなかった

### 対策
- ターミナルで実行するコマンドを提示する際は、**フォルダ移動（`cd`）から実行まで、そのまま貼り付けて動く形で1セットにして提示する**
  ```bash
  # 悪い例（不完全）
  firebase deploy --only functions

  # 良い例（完全）
  cd /該当フォルダのパス
  firebase deploy --only functions
  ```
- 「〜してから実行」と文章で説明するだけでなく、コマンド例にも必ず反映する

---

## #004 · 2026-06-08 · 関数を「即時関数(IIFE)の中」に定義し、外から呼んでReferenceErrorになった

### 何が起きたか
パフォーマンス改善で、統計のキャッシュ用関数 `_memoStat` / `_invalidateStudyStats`
/ `_readStudyData` を追加した。これらを `index.html` のアナリティクス用の
即時関数（IIFE：書いたそばから1回だけ実行される、外から中身が見えない箱）の
**内側**に定義してしまった。
一方で、これらの関数を IIFE の**外側**にある `save()` / `LS.set('study-data')` /
`allTimeTotal()` などからも呼んでいたため、外側からは関数が見えず、ブラウザで
起動した瞬間に `ReferenceError: _memoStat is not defined` /
`_invalidateStudyStats is not defined` が発生。統計表示・同期・保存・起動が不安定になった。

### 原因
- 既存の統計関数（`getTotalStudyMin` 等）がたまたまアナリティクス用IIFEの内側に
  あったため、その並びに新しい関数を置いた。だがその関数を「IIFEの外側」からも
  呼ぶ設計だったため、**変数・関数の見える範囲（スコープ）がずれた**。
- 確認を `node --check`（構文チェック）と `new Function()` での構文確認だけで
  済ませた。**構文チェックは文法エラーは見つけるが、スコープの誤り・未定義関数の
  呼び出し（ReferenceError）は検出できない**。
- 実際にブラウザ（または相当環境）で動かす確認をせずに「完了」と報告した。

### 対策
- **共有して使う関数・変数は、呼び出す全箇所から見える「最も外側のスコープ」に
  定義する**。IIFE（即時関数）の中は外から見えない、を常に意識する。
  - 内側の関数は外側の変数を参照できる（クロージャ）が、外側は内側を参照できない。
- **`var` で初期化する共有変数は、それを使う IIFE が実行されるより前の行に置く**
  （関数宣言は巻き上げで先に使えるが、`var` の代入はその行に到達するまで `undefined`）。
- コードを書き換えたら、構文チェックだけで終わらせず、**実際に実行して
  ReferenceError などが出ないかを確認する**。本リポジトリには検査スクリプトを用意した：
  ```bash
  cd /該当フォルダのパス
  npm install --no-save jsdom
  NODE_PATH=./node_modules node tools/verify-runtime.js
  ```
  「アプリ由来エラーなし ✓」と出ることを確認してから完了報告する。
- 「ブラウザで触っての確認まではできていない」場合は、その旨を正直に伝える
  （勝手に「動作確認済み」と言わない）。

## #005 · 2026-06-25 · 画面の文言を「固定HTML」だけ直し、実際の出どころ（JS）を見落とした

### 何が起きたか
更新案内ポップアップ（バージョンアップのお知らせ）の文面を新バージョン用に
書き換えるよう依頼された。`index.html` の固定HTML部分
（`<div id="update-version-text">…</div>` など、214〜216行付近）を直して
「更新した」と報告した。
しかし実際にユーザーの画面に出る文言は、その固定HTMLではなく、JavaScript の
`UPDATE_COPY`（6984〜6988行付近）から表示時に上書きされていた
（`showUpdateBanner` 内で `title.textContent = UPDATE_COPY.title` 等）。
そのため直したつもりの文言は反映されず、ユーザーの端末には**古い文面のまま**
ポップアップが表示された。バージョン番号を上げてお知らせする目的そのものが
果たせなかった。

### 原因
- 画面に出る文言を、**「見た目上それらしい置き場所（固定HTML）」だけ見て直し、
  その文言が実行時に実際どこから入るか（データの出どころ）を追跡しなかった**。
- 同じ文字列が**2か所（固定HTMLの初期値と `UPDATE_COPY`）に重複**しており、
  どちらが「実際に表示される本物」かを確認しなかった。重複が「直したつもり」を生んだ。
- `getElementById('update-version-text')` や `.textContent` / `.innerHTML` を
  検索していれば、JS（`UPDATE_COPY`）で上書きされていることにすぐ気づけたのに、
  その確認を省いた。
- 変更後、**実際にポップアップを表示させて目視確認**していない。構文チェックや
  `verify-runtime.js` はバナー表示の経路を通らないため、この種の「反映されない」は
  検出できない（#004と同根：チェックの種類と確認したい内容が噛み合っていない）。

### 対策
- **画面に出る文言・数値を変更するときは、その要素ID（や近くの文字列）が
  JSから上書きされていないか必ず検索する**。例：
  ```bash
  # 対象IDがJSで操作されていないか（textContent/innerHTML/value）
  grep -nE "getElementById\('update-version-text'\)|update-version-text" index.html
  ```
  固定HTMLは「初期値・フォールバック」であることが多く、**本物の出どころ（source of truth）は
  別のJS定数・変数**にある、と疑う。
- 同じ文字列が複数箇所にある場合、**実際に使われる単一の出どころを特定してから直す**。
  重複している全箇所をそろえる（今回なら `UPDATE_COPY` と固定HTMLの両方）。
- **表示系の修正は、実際にその表示を発生させて目視で確認**してから完了報告する。
  画面確認ができない場合は「コード上は直したが画面確認まではできていない」と正直に伝える。
- 「ユーザーに見える文言」のリリースは、**デプロイ後に実機（または相当環境）で1度確認**する。

### 補足（このポップアップ特有の設計上の注意）
- 更新ポップアップは「**今その端末で動いている（=古い）版**」が描画し、文面も
  その版の `UPDATE_COPY` を表示する。さらに本体HTMLはネット優先で取得されるため、
  多くの利用者は**ポップアップなしで静かに最新版へ入れ替わる**。
- つまり、このポップアップは「新しい文面を全員に確実に見せる」用途には向かない。
  全員に確実にお知らせしたい場合は、**version.json等を唯一の出どころにして
  “常に最新のお知らせ”を表示する別の仕組み（お知らせ機能）**を用意する方が確実。

---

## #006 · 2026-08-11 · 「画面外に隠した入力欄」でカレンダーを開き、パソコンでは何も出なかった

### 何が起きたか
ヘッダーの「今日の日付」（`current-date-display`）と「開花日」（`exam-date-display`）を
押すと日付を選べる、という機能。実体は画面外に隠した `<input type="date">`
（`current-date-input` は `left:-9999px`、`cd-input` は親の `.countdown-bar` ごと
`left:-9999px`）に対して `showPicker()` を呼ぶ作りだった。

スマホでは正常に動いていたが、**パソコン版では押しても何も起きない**という
不具合になっていた。ユーザーは過去の日付の入力ができず困っていた。

### 原因
- **カレンダーの出かたが機器によって違うことを見落とした**。
  - スマホ／タブレット：画面中央や下からせり上がる「別画面」で出る → 入力欄の位置は無関係。
  - パソコン（マウス操作）：**その入力欄のすぐ下にぶら下げて**出る
    → 入力欄が `left:-9999px` にあると、カレンダーも画面外に描画されて見えない。
- `showPicker()` は**エラーを出さずに成功扱いで返る**ため、コード上の
  「失敗したらポップアップを出す」フォールバックが一切働かなかった
  （＝失敗を検知できない作りだった）。
- スマホでの動作確認だけで完了としており、**パソコン（マウス操作）での確認をしていなかった**。

### 対策
- **画面外に隠した要素に対して `showPicker()` を呼ばない**。呼ぶ前に
  「その入力欄が本当に画面内に表示されているか」を `getBoundingClientRect()` で確認する。
  マウス操作の環境（`matchMedia('(pointer: fine)')`）で入力欄が画面外なら、
  必ず自前の選択ポップアップ（`_openDateEditPopup`）を使う。
- **成功したかどうか分からないAPIをフォールバックの条件にしない**。
  「エラーが出なかった＝ユーザーに見えている」ではない。
- **UIの修正は、スマホ想定とパソコン想定の両方で確認する**。本リポジトリでは
  playwright-core ＋ `/opt/pw-browsers/chromium-1194` で、
  デスクトップ表示とスマホ表示（`devices['Pixel 7']`）の両方を再現して確認できる。

---

## #007 · 2026-08-12 · 長いお知らせを「大きい画面でだけ」確認し、小さい携帯でOKボタンに届かなかった

### 何が起きたか
8/13のCrazy10開催を告知するため `notice.json` に本文＋箇条書き7項目の長めのお知らせを書いた。
お知らせのポップアップ（`#notice-overlay` の中の箱）には**高さの上限もスクロール指定も無かった**ため、
中身が画面より縦に長くなった端末では箱ごと画面からはみ出し、
**一番下の「OK！読んだっぺ」ボタンに指が届かない**状態になった。
利用者はお知らせを閉じられずに困った。

実測（修正前）：360×640の画面で箱の高さ850px、箱の上端 -105px、OKボタンの下端717px（＝画面外）。

### 原因
- 表示確認を **390×844（大きめのスマホ）と 1280×900（パソコン）でしか行っていなかった**。
  この2つでは、たまたま収まってしまい問題が見えなかった。
- 「表示させて目視確認した」ことで満足し、**画面が小さい側の端（375×667・360×640・320×568）を試さなかった**。
  教訓#006で「スマホ想定とパソコン想定の両方で確認する」としたが、
  **同じスマホでも大きい機種と小さい機種で結果が変わる**という視点が抜けていた。
- 文章量を自分で増やしたのに、**増やした結果として高さがどうなるかを確かめなかった**。
  お知らせは `notice.json` 次第でいくらでも長くなり得る＝可変なのに、器の側に上限が無かった。

### 対策
- **画面に出る箱（モーダル・ポップアップ）には必ず高さの上限とスクロールを付ける**。
  本リポジトリの4つ（`notice-overlay` の箱／`crazy10-popup`／`asajuku-popup`／`crazy10-finale-popup`）には
  以下を入れてある。**新しくポップアップを追加するときも必ず同じ指定を入れること**。
  ```css
  max-height:calc(100vh - 32px);
  max-height:calc(100dvh - 32px);  /* 携帯のアドレスバー分を含めた実際の高さ */
  overflow-y:auto;
  overscroll-behavior:contain;     /* 後ろの画面が一緒に動かないように */
  -webkit-overflow-scrolling:touch;
  ```
  （`overlay` 側の `padding` が16pxなら32px、20pxなら40pxを引く）
- **表示の確認は「小さい画面」から始める**。最低でも 320×568・360×640・375×667 を見る。
  確認の合格条件は「見えること」ではなく、**一番下のボタンを実際にクリックして閉じられること**。
- 文章量が可変なもの（`notice.json` のように外から差し替わるもの）は、
  **長い文章を入れた状態で**確認する。短い例文で確認しても意味がない。

---

## #008 · 2026-08-18 · バージョンを上げたら、5世代前（v113）の更新案内が利用者に出た

### 何が起きたか
v118（軌跡のしおり）をリリースし、手順どおり `version.json` / `sw.js` / `index.html` の
バージョンを上げた。`notice.json` の新しいお知らせは正しく表示された。
しかし利用者の端末には、それとは別に **v113の更新案内ポップアップ**が出た。
（固定HTML側の初期値に至ってはv110のままだった）

### 原因
- 更新案内の文面（`UPDATE_COPY`）は、**更新前の＝古い版が自分の中に持っている文字列**を表示する
  （教訓#005で分かっていた性質）。端末で動いていたv117が持っていた文面がv113のままだったため、
  v113の内容が表示された。
- つまり `UPDATE_COPY` に**版ごとの内容を書く設計そのものが間違っていた**。
  リリースのたびに書き換えても、表示されるのは常に「1つ前の版が持っている文面」なので、
  **どうやっても内容がズレる**。v113以降、誰も更新しなかったため5世代分ズレた。
- リリース手順（`sw.js` の先頭コメント）が「バージョンを上げる3か所」しか書いておらず、
  `UPDATE_COPY` の存在が手順から抜け落ちていた。

### 対策
- **`UPDATE_COPY` にバージョン名・新機能の内容を書かない**。
  「新しい版があります／更新しても記録は消えません」のような、**いつ表示されても正しい文面**にする
  （2026-08-18にそう書き換え済み。固定HTML側の初期値もそろえた）。
- **「何が新しいか」の案内は `notice.json` だけの仕事**にする。
  こちらは毎回ネットから取得するため、常に最新で、全員に確実に届く。
- 文面を直すときは**バージョンを上げない**。本体HTMLはネット優先で取得されるため、
  更新案内を出さずに次回起動で静かに反映される。バージョンを上げると、
  直っていない古い端末にまた古い案内が出てしまう。

---

## #009 · 2026-08-23 · 更新案内が「押すまで毎回」出続けていた（既読の記録を自分で消していた）

### 何が起きたか
v118を出した5日後、利用者から「更新のポップアップがまた出た」と報告があった。
調べると、**「今すぐ更新する」を押さずに閉じた端末では、開くたびに毎回出る**状態だった
（ブラウザで4回開いて4回とも表示を確認）。

### 原因
アプリの入れ替えは2段階ある。

1. 画面のファイル（`index.html`）… ネット優先取得なので、開いた瞬間に新しくなる
2. Service Worker（画像などを手元に保管する裏の仕組み）… **利用者がボタンを押すまで入れ替わらない**

`checkVersionJson()` は「`version.json` の版 ＝ いま動いている `index.html` の版」なら
**最新版とみなして既読記録（`UPDATE_PROMPT_SEEN_KEY`）を消していた**。
ところが1だけ新しく2が待機中という状態が普通に起こるため、
起動のたびに「既読記録が消える → 待機中のSWを見つけてバナーを出す」を繰り返していた。

**「2つの更新経路があり、片方だけ完了している状態」を想定していなかったのが原因。**

### 対策
- 既読記録を消すのは、**入れ替え待ちのService Workerが無いときだけ**にした
  （`_clearUpdatePromptIfNoWaitingWorker()`）。案内は1つの版につき1回で止まる。
- 案内を1回に減らすと、見逃した人が入れ替えを完了できなくなる。そこで
  **画面右上の「更新」ボタンは、版が同じでも入れ替え待ちがあれば案内を出す**ようにした
  （逃げ道を必ず1つ残す）。
- 記録のキーがブレると「同じ版なのに別物」と見なされ再び毎回出るため、
  版名が取れないときは `CURRENT_VERSION` を使うようにそろえた。
- **確認は「1回出るか」ではなく「2回目に出ないか」まで見る。**
  この種の不具合は初回だけ見ても絶対に気づけない。ブラウザで
  「古いSWを入れる → 新しいsw.jsを配る → 開き直しを3回」まで再現して確かめること。

---

## #010 · 2026-08-23 · 「日付が2桁にそろっている」前提に頼りながら、外から来るデータを検査していなかった

### 何が起きたか
「軌跡のしおり」の期間判定は、日付を**文字の並び順**で比べている（`ds < st` など6か所）。

```js
if (ds < st) continue;   // '2026-08-01' より前か？
```

これは**すべての日付キーが `YYYY-MM-DD`（月日が2桁）である**ことが前提。
`2026-1-5` のようにそろっていないと、文字としては `2026-08-01` より**後ろ**に来るため
（`1` > `0`）、1月の記録がまるごと「いまの旅」に入ってしまう。

実害は出ていない（アプリ自身は `padStart` で必ず2桁にそろえており、
`index.html` / `kotsu-tasks.js` のどこにもそろえない書き方は無い）。
だが**その前提をコードが検査していなかった**。

### 原因
外からデータが入ってくる唯一の入口である「バックアップから復元」が、
ファイルの中身を**そのまま** `study-data` に流し込んでいた。
手を加えたファイルや別の場所で作られたファイルを復元すると、静かに集計が狂う。

### 対策
- 復元処理に**関所**を1つ置いた（`_normalizeDateKey` / `_normalizeDateKeyedMap`）。
  `YYYY-M-D`・`YYYY/M/D` を `YYYY-MM-DD` にそろえ、同じ日が両方の書き方で入っていたら1つにまとめる。
  対象は `studyData` / `studyDeletes` / `studySubjects` / `studyNotes` の4つ。
- **日付として読めないキーは取り込まない。** アプリはどの画面でも日付で記録を探すため、
  取り込んでも二度と表示できず、合計だけを増やす「幽霊の記録」になる。
  直した件数・取り込まなかった件数はトーストで知らせる。
- **前提に頼るコードを書いたら、その前提が崩れる入口を必ず塞ぐこと。**
  「アプリ自身は正しく書いている」は、外から入る道がある限り保証にならない。

---

## #010 · 2026-08-19 · 更新案内ポップアップを「自動では出さない」ことにした

※ #009（更新案内が「押すまで毎回」出続けた不具合）は、コード内のコメントにのみ記録されている。

### 何が起きたか
Crazy11（8/21・8/22）のリリースを本番に反映したところ、利用者の端末に
お知らせ（notice.json）とは別に「🌸 新しい版があります」の更新案内ポップアップが出た。
利用者から「これは必要ない」と指摘があった。

### なぜ出たのか（不具合ではなく、設計そのものの問題）
このポップアップは **「更新前の＝古い版」が自分で描いている**。
version.json（＝新しい版の番号）を取りに行き、自分が持っている番号と違えば出る仕組みだった。
つまり**リリースするたび、まだ更新していない端末に必ず1回出る**。

しかし本体HTML（index.html）は**ネットワーク優先**で取得されるため、
**何も押さなくても次にオンラインで開いた時点で最新版に入れ替わる**。
つまりこのポップアップは、利用者に「押さなくてもいい操作」をお願いしていただけだった。
（#005・#008 で「文面が必ず古くなる」問題は分かっていたが、
　そもそも自動で出す必要が無い、というところまで踏み込めていなかった）

### 対策（2026-08-19に実施）
- **自動では一切出さない**。以下3つの自動表示をやめた（`index.html`）
  - `checkVersionJson()` の version.json 比較による表示
  - Service Worker の `updatefound` による表示
  - すでに待機中の Service Worker（`reg.waiting`）による表示
- 新しい Service Worker が用意できたら、**案内を出さずに黙って入れ替える**
  （`SKIP_WAITING` を送るだけ。画面の再読み込みはしない＝入力中の操作を邪魔しない）
- **設定の「更新」ボタン（`checkAppUpdate`）からだけは今まで通り出す**。
  入れ替わらない端末の逃げ道は残しておく必要があるため、ここは消さない。
- 「何が新しいか」の案内は **`notice.json` だけの仕事**（#008 の方針どおり）

### この変更を戻さないこと
`showUpdateBanner()` を自動の経路から呼び直すと、リリースのたびに全員に
不要なポップアップが出る状態に戻る。呼んでよいのは `checkAppUpdate`（利用者が自分で押す）だけ。

### 補足：この種の修正でバージョンを上げてはいけない
更新案内は「古い版が持っている文面・仕組み」で描かれる。
ここでバージョンを上げると、**まだ更新していない端末に「消したはずの案内」がもう1回出る**。
本体HTMLはネット優先なので、**バージョンを上げなくても次回起動で静かに反映される**（#008 と同じ理由）。

<!-- 新しい失敗は上記の形式でこの下に追記していく -->
