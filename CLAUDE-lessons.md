# Claude 失敗記録・原因・対策

作業前に必ず読むこと。同じミスを繰り返さないための記録。

---

## #001 · 2026-06-03 · 古いコードを「最新」として分析した

### 何が起きたか
ユーザーがローカル（自分のパソコン）で修正済みのコードについて質問した。
Claudeはリモート（GitHub上）の古いコードを見て「`kotsusaku-kiseki-cache` というキーは存在しない」と断言した。
しかし実際にはユーザーのローカルにすでに実装されており、回答が誤りだった。

### 原因
- 作業開始前に `git fetch` でGitHubの最新情報を取得しなかった
- ユーザーが「実装した」と言ったにもかかわらず、GitHubへの反映状況を確認しなかった
- リポジトリにあるコードをそのまま「最新版」と思い込んだ

### 対策
- GitHubのファイルに関する作業・質問・分析を始める前に、**毎回必ず**以下を実行する
  ```bash
  git fetch origin
  git log --oneline -5
  git status
  ```
- ユーザーが「実装した」「修正した」「変更した」と言ったら、GitHubに反映済みか確認してから回答する
- 確認せずに断定的な回答をしない

---

## #002 · 2026-06-03 · 専門用語を説明なしで使い続けた

### 何が起きたか
「プッシュ」「ブランチ」「コミット」「デプロイ」などの専門用語を、説明なしで繰り返し使い続けた。
ユーザーは非エンジニアであり、会話の中で何度も指摘を受けていたにもかかわらず徹底できていなかった。

### 原因
- セッションをまたぐと記憶がリセットされるため、以前の指摘が引き継がれなかった
- 「分かるだろう」という思い込みで説明を省略した

### 対策
- 専門用語を使う際は**必ず括弧書きで平易な説明を添える**
  - 例：「プッシュ（自分のパソコンからGitHubに変更を送り込む作業）」
  - 例：「ブランチ（作業用の分岐・コピー）」
  - 例：「コミット（変更内容をひとまとまりとして記録すること）」
  - 例：「デプロイ（作ったものを本番環境に反映させる作業）」
- 技術的な説明はたとえ話や日常語に置き換える

---

## #003 · 2026-06-03 · コマンド例にフォルダ移動（cd）を含めなかった

### 何が起きたか
「フォルダに移動してから実行してください」と文章では説明したにもかかわらず、
コマンド例として `firebase deploy --only functions` の1行だけを提示した。
ターミナル（コマンド入力画面）を開いた直後は別のフォルダにいるため、
移動コマンド（`cd`）を含めないと正しく実行できない。

### 原因
- 「文章で説明したから伝わるだろう」という思い込みでコマンド例を省略した
- 非エンジニアのユーザーにとって、文章の説明とコマンド例が別々だと混乱しやすいことへの配慮が足りなかった

### 対策
- ターミナルで実行するコマンドを提示する際は、**フォルダ移動（`cd`）から実行まで、そのまま貼り付けて動く形で1セットにして提示する**
  ```bash
  # 悪い例（不完全）
  firebase deploy --only functions

  # 良い例（完全）
  cd /該当フォルダのパス
  firebase deploy --only functions
  ```
- 「〜してから実行」と文章で説明するだけでなく、コマンド例にも必ず反映する

---

<!-- 新しい失敗は上記の形式でこの下に追記していく -->
