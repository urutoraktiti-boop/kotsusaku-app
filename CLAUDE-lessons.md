# Claude 失敗記録・原因・対策

作業前に必ず読むこと。同じミスを繰り返さないための記録。

---

## #001 · 2026-06-03 · 古いコードを「最新」として分析した

### 何が起きたか
ユーザーがローカル（自分のパソコン）で修正済みのコードについて質問した。
Claudeはリモート（GitHub上）の古いコードを見て「`kotsusaku-kiseki-cache` というキーは存在しない」と断言した。
しかし実際にはユーザーのローカルにすでに実装されており、回答が誤りだった。

### 原因
- 作業開始前に `git fetch` でGitHubの最新情報を取得しなかった
- ユーザーが「実装した」と言ったにもかかわらず、GitHubへの反映状況を確認しなかった
- リポジトリにあるコードをそのまま「最新版」と思い込んだ

### 対策
- GitHubのファイルに関する作業・質問・分析を始める前に、**毎回必ず**以下を実行する
  ```bash
  git fetch origin
  git log --oneline -5
  git status
  ```
- ユーザーが「実装した」「修正した」「変更した」と言ったら、GitHubに反映済みか確認してから回答する
- 確認せずに断定的な回答をしない

---

## #002 · 2026-06-03 · 専門用語を説明なしで使い続けた

### 何が起きたか
「プッシュ」「ブランチ」「コミット」「デプロイ」などの専門用語を、説明なしで繰り返し使い続けた。
ユーザーは非エンジニアであり、会話の中で何度も指摘を受けていたにもかかわらず徹底できていなかった。

### 原因
- セッションをまたぐと記憶がリセットされるため、以前の指摘が引き継がれなかった
- 「分かるだろう」という思い込みで説明を省略した

### 対策
- 専門用語を使う際は**必ず括弧書きで平易な説明を添える**
  - 例：「プッシュ（自分のパソコンからGitHubに変更を送り込む作業）」
  - 例：「ブランチ（作業用の分岐・コピー）」
  - 例：「コミット（変更内容をひとまとまりとして記録すること）」
  - 例：「デプロイ（作ったものを本番環境に反映させる作業）」
- 技術的な説明はたとえ話や日常語に置き換える

---

## #003 · 2026-06-03 · コマンド例にフォルダ移動（cd）を含めなかった

### 何が起きたか
「フォルダに移動してから実行してください」と文章では説明したにもかかわらず、
コマンド例として `firebase deploy --only functions` の1行だけを提示した。
ターミナル（コマンド入力画面）を開いた直後は別のフォルダにいるため、
移動コマンド（`cd`）を含めないと正しく実行できない。

### 原因
- 「文章で説明したから伝わるだろう」という思い込みでコマンド例を省略した
- 非エンジニアのユーザーにとって、文章の説明とコマンド例が別々だと混乱しやすいことへの配慮が足りなかった

### 対策
- ターミナルで実行するコマンドを提示する際は、**フォルダ移動（`cd`）から実行まで、そのまま貼り付けて動く形で1セットにして提示する**
  ```bash
  # 悪い例（不完全）
  firebase deploy --only functions

  # 良い例（完全）
  cd /該当フォルダのパス
  firebase deploy --only functions
  ```
- 「〜してから実行」と文章で説明するだけでなく、コマンド例にも必ず反映する

---

## #004 · 2026-06-08 · 関数を「即時関数(IIFE)の中」に定義し、外から呼んでReferenceErrorになった

### 何が起きたか
パフォーマンス改善で、統計のキャッシュ用関数 `_memoStat` / `_invalidateStudyStats`
/ `_readStudyData` を追加した。これらを `index.html` のアナリティクス用の
即時関数（IIFE：書いたそばから1回だけ実行される、外から中身が見えない箱）の
**内側**に定義してしまった。
一方で、これらの関数を IIFE の**外側**にある `save()` / `LS.set('study-data')` /
`allTimeTotal()` などからも呼んでいたため、外側からは関数が見えず、ブラウザで
起動した瞬間に `ReferenceError: _memoStat is not defined` /
`_invalidateStudyStats is not defined` が発生。統計表示・同期・保存・起動が不安定になった。

### 原因
- 既存の統計関数（`getTotalStudyMin` 等）がたまたまアナリティクス用IIFEの内側に
  あったため、その並びに新しい関数を置いた。だがその関数を「IIFEの外側」からも
  呼ぶ設計だったため、**変数・関数の見える範囲（スコープ）がずれた**。
- 確認を `node --check`（構文チェック）と `new Function()` での構文確認だけで
  済ませた。**構文チェックは文法エラーは見つけるが、スコープの誤り・未定義関数の
  呼び出し（ReferenceError）は検出できない**。
- 実際にブラウザ（または相当環境）で動かす確認をせずに「完了」と報告した。

### 対策
- **共有して使う関数・変数は、呼び出す全箇所から見える「最も外側のスコープ」に
  定義する**。IIFE（即時関数）の中は外から見えない、を常に意識する。
  - 内側の関数は外側の変数を参照できる（クロージャ）が、外側は内側を参照できない。
- **`var` で初期化する共有変数は、それを使う IIFE が実行されるより前の行に置く**
  （関数宣言は巻き上げで先に使えるが、`var` の代入はその行に到達するまで `undefined`）。
- コードを書き換えたら、構文チェックだけで終わらせず、**実際に実行して
  ReferenceError などが出ないかを確認する**。本リポジトリには検査スクリプトを用意した：
  ```bash
  cd /該当フォルダのパス
  npm install --no-save jsdom
  NODE_PATH=./node_modules node tools/verify-runtime.js
  ```
  「アプリ由来エラーなし ✓」と出ることを確認してから完了報告する。
- 「ブラウザで触っての確認まではできていない」場合は、その旨を正直に伝える
  （勝手に「動作確認済み」と言わない）。

## #005 · 2026-06-25 · 画面の文言を「固定HTML」だけ直し、実際の出どころ（JS）を見落とした

### 何が起きたか
更新案内ポップアップ（バージョンアップのお知らせ）の文面を新バージョン用に
書き換えるよう依頼された。`index.html` の固定HTML部分
（`<div id="update-version-text">…</div>` など、214〜216行付近）を直して
「更新した」と報告した。
しかし実際にユーザーの画面に出る文言は、その固定HTMLではなく、JavaScript の
`UPDATE_COPY`（6984〜6988行付近）から表示時に上書きされていた
（`showUpdateBanner` 内で `title.textContent = UPDATE_COPY.title` 等）。
そのため直したつもりの文言は反映されず、ユーザーの端末には**古い文面のまま**
ポップアップが表示された。バージョン番号を上げてお知らせする目的そのものが
果たせなかった。

### 原因
- 画面に出る文言を、**「見た目上それらしい置き場所（固定HTML）」だけ見て直し、
  その文言が実行時に実際どこから入るか（データの出どころ）を追跡しなかった**。
- 同じ文字列が**2か所（固定HTMLの初期値と `UPDATE_COPY`）に重複**しており、
  どちらが「実際に表示される本物」かを確認しなかった。重複が「直したつもり」を生んだ。
- `getElementById('update-version-text')` や `.textContent` / `.innerHTML` を
  検索していれば、JS（`UPDATE_COPY`）で上書きされていることにすぐ気づけたのに、
  その確認を省いた。
- 変更後、**実際にポップアップを表示させて目視確認**していない。構文チェックや
  `verify-runtime.js` はバナー表示の経路を通らないため、この種の「反映されない」は
  検出できない（#004と同根：チェックの種類と確認したい内容が噛み合っていない）。

### 対策
- **画面に出る文言・数値を変更するときは、その要素ID（や近くの文字列）が
  JSから上書きされていないか必ず検索する**。例：
  ```bash
  # 対象IDがJSで操作されていないか（textContent/innerHTML/value）
  grep -nE "getElementById\('update-version-text'\)|update-version-text" index.html
  ```
  固定HTMLは「初期値・フォールバック」であることが多く、**本物の出どころ（source of truth）は
  別のJS定数・変数**にある、と疑う。
- 同じ文字列が複数箇所にある場合、**実際に使われる単一の出どころを特定してから直す**。
  重複している全箇所をそろえる（今回なら `UPDATE_COPY` と固定HTMLの両方）。
- **表示系の修正は、実際にその表示を発生させて目視で確認**してから完了報告する。
  画面確認ができない場合は「コード上は直したが画面確認まではできていない」と正直に伝える。
- 「ユーザーに見える文言」のリリースは、**デプロイ後に実機（または相当環境）で1度確認**する。

### 補足（このポップアップ特有の設計上の注意）
- 更新ポップアップは「**今その端末で動いている（=古い）版**」が描画し、文面も
  その版の `UPDATE_COPY` を表示する。さらに本体HTMLはネット優先で取得されるため、
  多くの利用者は**ポップアップなしで静かに最新版へ入れ替わる**。
- つまり、このポップアップは「新しい文面を全員に確実に見せる」用途には向かない。
  全員に確実にお知らせしたい場合は、**version.json等を唯一の出どころにして
  “常に最新のお知らせ”を表示する別の仕組み（お知らせ機能）**を用意する方が確実。

---

## #006 · 2026-08-11 · 「画面外に隠した入力欄」でカレンダーを開き、パソコンでは何も出なかった

### 何が起きたか
ヘッダーの「今日の日付」（`current-date-display`）と「開花日」（`exam-date-display`）を
押すと日付を選べる、という機能。実体は画面外に隠した `<input type="date">`
（`current-date-input` は `left:-9999px`、`cd-input` は親の `.countdown-bar` ごと
`left:-9999px`）に対して `showPicker()` を呼ぶ作りだった。

スマホでは正常に動いていたが、**パソコン版では押しても何も起きない**という
不具合になっていた。ユーザーは過去の日付の入力ができず困っていた。

### 原因
- **カレンダーの出かたが機器によって違うことを見落とした**。
  - スマホ／タブレット：画面中央や下からせり上がる「別画面」で出る → 入力欄の位置は無関係。
  - パソコン（マウス操作）：**その入力欄のすぐ下にぶら下げて**出る
    → 入力欄が `left:-9999px` にあると、カレンダーも画面外に描画されて見えない。
- `showPicker()` は**エラーを出さずに成功扱いで返る**ため、コード上の
  「失敗したらポップアップを出す」フォールバックが一切働かなかった
  （＝失敗を検知できない作りだった）。
- スマホでの動作確認だけで完了としており、**パソコン（マウス操作）での確認をしていなかった**。

### 対策
- **画面外に隠した要素に対して `showPicker()` を呼ばない**。呼ぶ前に
  「その入力欄が本当に画面内に表示されているか」を `getBoundingClientRect()` で確認する。
  マウス操作の環境（`matchMedia('(pointer: fine)')`）で入力欄が画面外なら、
  必ず自前の選択ポップアップ（`_openDateEditPopup`）を使う。
- **成功したかどうか分からないAPIをフォールバックの条件にしない**。
  「エラーが出なかった＝ユーザーに見えている」ではない。
- **UIの修正は、スマホ想定とパソコン想定の両方で確認する**。本リポジトリでは
  playwright-core ＋ `/opt/pw-browsers/chromium-1194` で、
  デスクトップ表示とスマホ表示（`devices['Pixel 7']`）の両方を再現して確認できる。

<!-- 新しい失敗は上記の形式でこの下に追記していく -->
