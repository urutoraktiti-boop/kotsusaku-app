# このプロジェクトでのClaudeへの指示

## コミュニケーションのルール

- ユーザーは非エンジニアのため、専門用語を使う際は**必ず括弧書きで平易な説明を添えること**
  - 例：「プッシュ（自分のパソコンからGitHubに変更を送り込む作業）」
  - 例：「ブランチ（作業用の分岐・コピー）」
  - 例：「コミット（変更内容をひとまとまりとして記録すること）」
  - 例：「デプロイ（作ったものを本番環境に反映させる作業）」
- 技術的な説明はできるだけ日常語に置き換えること
- 難しい概念はたとえ話を使って説明すること

## 作業開始時のルール

- 作業を始める前に、必ずブランチ（作業用の分岐）の現在の状態を確認すること
  - `git fetch origin` でリモート（GitHub上）の最新情報を取得
  - `git log --oneline -5` で最新のコミット（変更履歴）を確認
  - `git status` で未コミット（未記録）の変更がないか確認
- ユーザーが「実装した」「修正した」と言った場合は、その変更がGitHubに反映済みかを確認してから分析を始めること
