# このプロジェクトでのClaudeへの指示

## コミュニケーションのルール

- ユーザーは非エンジニアのため、専門用語を使う際は**必ず括弧書きで平易な説明を添えること**
  - 例：「プッシュ（自分のパソコンからGitHubに変更を送り込む作業）」
  - 例：「ブランチ（作業用の分岐・コピー）」
  - 例：「コミット（変更内容をひとまとまりとして記録すること）」
  - 例：「デプロイ（作ったものを本番環境に反映させる作業）」
- 技術的な説明はできるだけ日常語に置き換えること
- 難しい概念はたとえ話を使って説明すること

## 作業開始時のルール

アプリ・ホームページ・ダッシュボードなど、GitHubで管理しているファイルに関する作業・質問・分析を始める際は、**必ず最初に以下の3つを実行して最新状態を確認すること**。

```bash
git fetch origin                  # GitHub上の最新情報を取得
git log --oneline -5              # 最新の変更履歴を確認
git status                        # 未記録の変更がないか確認
```

- ユーザーが「実装した」「修正した」「変更した」と言った場合は、その変更がGitHubに反映済みかを必ず確認してから分析・回答を始めること
- 確認せずに古いコードを見て回答することは禁止
