# このプロジェクトでのClaudeへの指示

## 必須：作業前に読むファイル

作業・質問・分析を始める前に、必ず `CLAUDE-lessons.md` を読むこと。
過去の失敗・原因・対策が記録されており、同じミスを繰り返さないために毎回確認する。

## 機能の仕組みドキュメント（実装前に確認）

主要機能の仕組みは `docs/` にまとめてある。関連する作業の前に該当ドキュメントを読むこと。

- `docs/notice-feature.md` … お知らせ機能（`notice.json` を唯一の出どころに、全員へ確実に表示する仕組み）。お知らせの出し方・注意点もここに記載。
- `docs/crazy10-feature.md` … クレイジー（ITACHACHA House Crazy10）イベント機能。**次回イベントの追加手順**・「回（series）」の考え方・フィナーレの仕組みを記載。イベント追加を頼まれたら必ず読むこと。
- `docs/minna-no-kiseki-feature.md` … 設定画面の「みんなの軌跡」（全体の総学習時間・コツ習得数）。データの流れ・**Firestoreの読み取り回数**・「数字が更新されない」ときの確認手順を記載。統計まわりを触る前に読むこと。
- `docs/season-reset-feature.md` … 「数え直しの区切り日」（8月までの記録を残したまま、9月から学習時間を0から数え直す機能）。データは消さず日付1つで切り替える仕組み・レベルやバッジを数え直さない理由を記載。累計表示や予測の集計範囲を触る前に読むこと。
- `docs/kotsu-spirit-feature.md` … コツ習慣「スピリット編」（100コツ到達後の第二部。能力表A〜E・スピリット8体・覚醒Lv/称号・タマゴ割れ覚醒演出）。データ構造・関数・画像・リリース手順を記載。

## コミュニケーションのルール

- ユーザーは非エンジニアのため、専門用語を使う際は**必ず括弧書きで平易な説明を添えること**
  - 例：「プッシュ（自分のパソコンからGitHubに変更を送り込む作業）」
  - 例：「ブランチ（作業用の分岐・コピー）」
  - 例：「コミット（変更内容をひとまとまりとして記録すること）」
  - 例：「デプロイ（作ったものを本番環境に反映させる作業）」
- 技術的な説明はできるだけ日常語に置き換えること
- 難しい概念はたとえ話を使って説明すること

## アプリ内の文章のルール（ポップアップ・お知らせ）

**画面に出す文章は短く。長い文章は小さい携帯でボタンに届かなくなる**（`CLAUDE-lessons.md` #007）。
器の側はスクロールできるよう直したが、**そもそもスクロールせずに読み切れる長さで書くこと**。

### 文字数の上限（`notice.json` などお知らせ全般）

| 項目 | 上限 | 補足 |
|---|---|---|
| `title` | **25文字** | 1行で言い切る |
| `body` | **90文字** | 2〜3文まで |
| `notes` | **4項目** | 1項目 **30文字**まで |
| 合計 | **250文字** | これを超えたら削る |

### 書くときの考え方

- **「読まないと困ること」だけ書く**。あれば嬉しい程度の情報は載せない
- 1項目に2つのことを詰めない。詰めたくなったら、優先度の低い方を消す
- 設定画面の場所の案内（「設定→○○→△△」）は毎回書かない。必要なときだけ1回
- 世界観・キャラの味付けは `body` に1文だけ。`notes` は事実だけを短く
- 書いたら**必ず文字数を数えて確認する**
  ```bash
  cd /該当フォルダのパス
  python3 -c "
  import json; d=json.load(open('notice.json'))
  print('title',len(d['title']),'/ body',len(d['body']),'/ notes',len(d['notes']),'項目')
  for n in d['notes']: print(' ',len(n),n)
  print('合計',len(d['title'])+len(d['body'])+sum(len(n) for n in d['notes']))"
  ```
- 仕上げに**320×568（最小級の携帯）で実際に表示させ、スクロールせずに読めるか**を見る

## 作業開始時のルール

アプリ・ホームページ・ダッシュボードなど、GitHubで管理しているファイルに関する作業・質問・分析を始める際は、**必ず最初に以下の3つを実行して最新状態を確認すること**。

```bash
git fetch origin                  # GitHub上の最新情報を取得
git log --oneline -5              # 最新の変更履歴を確認
git status                        # 未記録の変更がないか確認
```

- ユーザーが「実装した」「修正した」「変更した」と言った場合は、その変更がGitHubに反映済みかを必ず確認してから分析・回答を始めること
- 確認せずに古いコードを見て回答することは禁止
